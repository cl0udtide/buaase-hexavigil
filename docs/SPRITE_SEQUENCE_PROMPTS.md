# 序列帧生成 Prompt 清单

本文件里的每一条 prompt 都可以直接复制使用。目标是生成 HexaVigil 当前战棋塔防画面可用的角色、敌人、Boss、建筑序列帧。

统一要求：透明背景 PNG，普通角色/敌人/建筑每帧 128x128，Boss 每帧 192x192；三分之二俯视伪 3D 视角；暗色战术奇幻科幻风格；主体居中偏下，底部中心作为锚点；缩小到 64px 地图格后轮廓仍清楚；不要文字、数字、Logo、水印、UI 边框、地面、背景、过强光效、纯黑剪影、Q版、正面头像、写实 3D 渲染。

## 第一批最优先

### guard_t1 idle

```text
生成一组 6-8 帧序列帧，用于游戏单位 guard_t1 的 idle 待机动画。角色是一个一阶近卫干员，紧凑的持剑战术角色，穿旧钢甲和深色短外套，有少量暗红色装饰，武器轮廓清晰。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 精灵，三分之二俯视伪 3D 视角，夜战氛围，深蓝灰阴影、灰褐金属、少量青蓝/暗红点缀。角色完整身体可见，脚部可见，主体居中偏下，底部中心锚点，透明背景，每帧 128x128 PNG。动画是轻微呼吸和布料/装备小幅摆动，双脚位置稳定，武器保持待命，不攻击。输出为独立连续帧，命名为 guard_t1_idle_000.png 到 guard_t1_idle_007.png。不要文字、数字、Logo、水印、背景、地面、UI、夸张光效、裁切身体、Q版、正面头像、写实 3D。
```

### guard_t1 attack

```text
生成一组 6-10 帧序列帧，用于游戏单位 guard_t1 的 attack 普攻动画。角色是一个一阶近卫干员，紧凑的持剑战术角色，穿旧钢甲和深色短外套，有少量暗红色装饰。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 精灵，三分之二俯视伪 3D 视角，透明背景，每帧 128x128 PNG。动作包含清晰的起手、短剑斩击、红橙色金属火花、收招，身体位置和尺寸在所有帧中保持一致，特效不要超过画布边缘。角色完整身体可见，脚部可见，主体居中偏下，底部中心锚点。输出为独立连续帧，命名为 guard_t1_attack_000.png 到 guard_t1_attack_009.png。不要文字、数字、Logo、水印、背景、地面、UI、过强光效、裁切身体、Q版、正面头像、写实 3D。
```

### sniper_t1 idle

```text
生成一组 6-8 帧序列帧，用于游戏单位 sniper_t1 的 idle 待机动画。角色是一个一阶狙击干员，使用紧凑步枪或弩，穿轻型战术外套，有蓝色瞄具和少量金属装具，轮廓要能看出远程单位。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 精灵，三分之二俯视伪 3D 视角，深蓝灰阴影、灰褐金属、少量冷蓝镜片光。角色完整身体可见，脚部可见，主体居中偏下，底部中心锚点，透明背景，每帧 128x128 PNG。动画是轻微呼吸、枪械小幅稳定移动、衣摆轻微摆动，双脚位置稳定。输出为 sniper_t1_idle_000.png 到 sniper_t1_idle_007.png。不要文字、数字、Logo、水印、背景、地面、UI、夸张枪火、裁切身体、Q版、正面头像、写实 3D。
```

### sniper_t1 attack

```text
生成一组 6-10 帧序列帧，用于游戏单位 sniper_t1 的 attack 普攻动画。角色是一个一阶狙击干员，使用紧凑步枪或弩，蓝色瞄具，轻型战术外套。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 精灵，三分之二俯视伪 3D 视角，透明背景，每帧 128x128 PNG。动作包含瞄准、扣动扳机或发射、短促淡蓝色枪口闪光/箭矢轨迹、后坐力、恢复待机。角色完整身体可见，主体居中偏下，底部中心锚点，缩小到 64px 后枪械轮廓仍清楚。输出为 sniper_t1_attack_000.png 到 sniper_t1_attack_009.png。不要文字、数字、Logo、水印、背景、地面、UI、巨大弹幕、过曝光效、裁切身体、Q版、正面头像、写实 3D。
```

### caster_t1 idle

```text
生成一组 6-8 帧序列帧，用于游戏单位 caster_t1 的 idle 待机动画。角色是一个一阶术士干员，拿短法杖，身旁有小型悬浮法环，穿轻型深色长外套，有少量青蓝符文光。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 精灵，三分之二俯视伪 3D 视角，深蓝灰阴影、灰褐装备、克制青蓝魔法光。角色完整身体可见，脚部可见，主体居中偏下，底部中心锚点，透明背景，每帧 128x128 PNG。动画是轻微呼吸、法环缓慢漂浮、符文微弱闪动，双脚位置稳定。输出为 caster_t1_idle_000.png 到 caster_t1_idle_007.png。不要文字、数字、Logo、水印、背景、地面、UI、大面积光效、裁切身体、Q版、正面头像、写实 3D。
```

### caster_t1 attack

```text
生成一组 6-10 帧序列帧，用于游戏单位 caster_t1 的 attack 普攻动画。角色是一个一阶术士干员，拿短法杖，带小型悬浮法环，穿轻型深色外套。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 精灵，三分之二俯视伪 3D 视角，透明背景，每帧 128x128 PNG。动作包含抬杖、青蓝魔法凝聚、小型法术弹释放、法环轻微旋转、收势。特效要克制，不能遮住身体，不能超出画布边缘。角色完整身体可见，主体居中偏下，底部中心锚点。输出为 caster_t1_attack_000.png 到 caster_t1_attack_009.png。不要文字、数字、Logo、水印、背景、地面、UI、过曝魔法、裁切身体、Q版、正面头像、写实 3D。
```

### defender_t1 idle

```text
生成一组 6-8 帧序列帧，用于游戏单位 defender_t1 的 idle 待机动画。角色是一个一阶重装干员，手持宽大的矩形盾，穿厚重护甲和强化靴，有少量黄绿色信号灯点缀，整体轮廓宽而稳。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 精灵，三分之二俯视伪 3D 视角，深蓝灰阴影、灰褐钢铁、克制黄绿光。角色完整身体可见，脚部可见，主体居中偏下，底部中心锚点，透明背景，每帧 128x128 PNG。动画是稳定防御姿态，轻微呼吸，盾牌和肩甲小幅移动，双脚不漂移。输出为 defender_t1_idle_000.png 到 defender_t1_idle_007.png。不要文字、数字、Logo、水印、背景、地面、UI、过强光效、裁切身体、Q版、正面头像、写实 3D。
```

### defender_t1 attack

```text
生成一组 6-10 帧序列帧，用于游戏单位 defender_t1 的 attack 普攻动画。角色是一个一阶重装干员，手持宽大矩形盾和短武器，穿厚重护甲。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 精灵，三分之二俯视伪 3D 视角，透明背景，每帧 128x128 PNG。动作包含盾前压、短武器或盾击、轻微黄绿色防御闪光、恢复防御姿态。动作幅度稳重，角色位置和比例保持一致，盾牌轮廓必须清楚。输出为 defender_t1_attack_000.png 到 defender_t1_attack_009.png。不要文字、数字、Logo、水印、背景、地面、UI、巨大冲击波、裁切身体、Q版、正面头像、写实 3D。
```

### slime idle

```text
生成一组 6-8 帧序列帧，用于敌人 slime 的 idle 待机动画。敌人是小型腐化源石虫/黏液怪，低矮半透明暗紫黑身体，内部有红紫色发光核心，轮廓简单，明显是弱小敌人。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 精灵，三分之二俯视伪 3D 视角，透明背景，每帧 128x128 PNG。动画是身体轻微蠕动、核心微弱闪烁、底部保持在同一位置。主体居中偏下，适配 64px 地图格。输出为 slime_idle_000.png 到 slime_idle_007.png。不要文字、数字、Logo、水印、背景、地面、UI、可爱卡通、巨大身体、写实 3D、过强光效。
```

### slime walk

```text
生成一组 6-8 帧序列帧，用于敌人 slime 的 walk 移动动画。敌人是小型腐化源石虫/黏液怪，低矮半透明暗紫黑身体，内部有红紫色发光核心。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 精灵，三分之二俯视伪 3D 视角，透明背景，每帧 128x128 PNG。动作是缓慢向前蠕动，身体压缩和伸展，核心轻微晃动，整体始终保持居中，不能产生位移出画布。输出为 slime_walk_000.png 到 slime_walk_007.png。不要文字、数字、Logo、水印、背景、地面、UI、可爱卡通、巨大身体、写实 3D、过强光效。
```

### hound idle

```text
生成一组 6-8 帧序列帧，用于敌人 hound 的 idle 待机动画。敌人是快速荒原猎犬，身体低伏、瘦长、暗色毛发或暗色外骨骼，有红色眼光和少量金属/晶体碎片，轮廓要体现高速敌人。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 精灵，三分之二俯视伪 3D 视角，透明背景，每帧 128x128 PNG。动画是低伏呼吸、爪子轻微抓地、头部警觉小幅移动，脚点稳定。主体居中偏下，适配 64px 地图格。输出为 hound_idle_000.png 到 hound_idle_007.png。不要文字、数字、Logo、水印、背景、地面、UI、可爱宠物感、巨大身体、写实 3D、过强光效。
```

### hound walk

```text
生成一组 6-8 帧序列帧，用于敌人 hound 的 walk 奔跑/移动动画。敌人是快速荒原猎犬，身体低伏、瘦长、暗色毛发或暗色外骨骼，有红色眼光和少量金属/晶体碎片。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 精灵，三分之二俯视伪 3D 视角，透明背景，每帧 128x128 PNG。动作是紧凑的低姿态奔跑循环，腿部清晰但不要太散，身体保持居中不出画布，缩小后仍能看出高速猎犬轮廓。输出为 hound_walk_000.png 到 hound_walk_007.png。不要文字、数字、Logo、水印、背景、地面、UI、可爱宠物感、巨大身体、写实 3D、过强光效。
```

### wood_wall idle

```text
生成 1 帧或 6 帧轻微循环序列帧，用于建筑 wood_wall 的 idle 默认状态。建筑是临时木制路障，强化木材、金属箍、旧钉板，轮廓宽而稳，能明显看出用于阻挡道路。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 建筑精灵，三分之二俯视伪 3D 视角，透明背景，每帧 128x128 PNG。主体居中偏下，底部中心锚点，适配 64px 地图格。如果做循环动画，只需要非常轻微的阴影和小木屑摆动，不要明显移动。输出为 wood_wall_idle_000.png 到 wood_wall_idle_005.png。不要文字、数字、Logo、水印、背景、地面、UI、过大阴影、写实 3D。
```

### medical_station idle

```text
生成 1 帧或 6 帧轻微循环序列帧，用于建筑 medical_station 的 idle 默认状态。建筑是小型战地医疗站，折叠支架、深色金属外壳、青蓝色治疗核心或小灯，不能使用文字和红十字标志，功能要通过柔和医疗光和设备造型表达。画风为暗色战术奇幻科幻塔防游戏的手绘 2D 建筑精灵，三分之二俯视伪 3D 视角，透明背景，每帧 128x128 PNG。主体居中偏下，底部中心锚点，适配 64px 地图格。如果做循环动画，青蓝灯光轻微脉冲，设备不要移动。输出为 medical_station_idle_000.png 到 medical_station_idle_005.png。不要文字、数字、Logo、水印、背景、地面、UI、红十字、过强光效、写实 3D。
```

## 我方干员补全

### 近卫通用 idle

```text
生成一组 6-8 帧序列帧，用于暗色战术奇幻科幻塔防游戏的近卫干员 idle 待机动画。角色是近战单位，完整身体可见，脚部可见，三分之二俯视伪 3D 视角，手绘 2D 精灵，透明背景，每帧 128x128 PNG。请根据以下 id 生成对应角色：guard_01 是二阶线防近卫，重外套和长战术刃；guard_t3 是三阶精英挥砍近卫，长外套和宽大武器；mountain 是拳斗近卫，强化手套和强壮轮廓；zuo_le 是敏捷决斗近卫，细身护甲和锋利刀刃；degenbrecher 是精英破刃近卫，黑钢外套和强势大剑；surtr 是火焰剑近卫，暗色护甲和克制橙色火焰大剑。每个角色都要主体居中偏下，底部中心锚点，双脚稳定，动画只做呼吸、衣摆和装备轻微摆动。输出文件用对应角色 id 命名，例如 guard_01_idle_000.png 到 guard_01_idle_007.png。不要文字、数字、Logo、水印、背景、地面、UI、裁切身体、Q版、正面头像、写实 3D、过强光效。
```

### 近卫通用 attack

```text
生成一组 6-10 帧序列帧，用于暗色战术奇幻科幻塔防游戏的近卫干员 attack 普攻动画。角色是近战单位，完整身体可见，脚部可见，三分之二俯视伪 3D 视角，手绘 2D 精灵，透明背景，每帧 128x128 PNG。请根据以下 id 生成对应角色：guard_01 是二阶线防近卫，重外套和长战术刃，琥珀色金属斩击；guard_t3 是三阶精英挥砍近卫，长外套和宽大武器，明亮琥珀斩弧；mountain 是拳斗近卫，强化手套，温暖冲击火花；zuo_le 是敏捷决斗近卫，细身护甲和刀刃，深红能量脉冲；degenbrecher 是精英破刃近卫，黑钢外套和大剑，白色钢铁斩击；surtr 是火焰剑近卫，暗色护甲和橙色火焰大剑。动作包含起手、攻击释放、命中感、收招，身体位置和比例保持一致，特效克制不遮挡身体。输出文件用对应角色 id 命名，例如 guard_01_attack_000.png 到 guard_01_attack_009.png。不要文字、数字、Logo、水印、背景、地面、UI、裁切身体、Q版、正面头像、写实 3D、过强光效。
```

### 狙击通用 idle

```text
生成一组 6-8 帧序列帧，用于暗色战术奇幻科幻塔防游戏的狙击干员 idle 待机动画。角色是远程物理单位，完整身体可见，脚部可见，三分之二俯视伪 3D 视角，手绘 2D 精灵，透明背景，每帧 128x128 PNG。请根据以下 id 生成对应角色：sniper_t2 是二阶精准射手，长枪和蓝色光学瞄具；archer_basic 是三阶重型远程干员，大型发射器或弓枪；narantuya 是机动太阳猎手，弧形武器和金色装饰；ray 是束缚光狙击，长枪管和白金光束主题；typhon 是重型猎手，厚重猎弩/枪械和冷蓝标记；wisadel 是精英饱和火力狙击，先进发射器和青蓝战术灯。动画只做呼吸、枪械小幅稳定移动、衣摆轻微摆动，双脚稳定，主体居中偏下，底部中心锚点。输出文件用对应角色 id 命名，例如 sniper_t2_idle_000.png 到 sniper_t2_idle_007.png。不要文字、数字、Logo、水印、背景、地面、UI、巨大弹幕、裁切身体、Q版、正面头像、写实 3D。
```

### 狙击通用 attack

```text
生成一组 6-10 帧序列帧，用于暗色战术奇幻科幻塔防游戏的狙击干员 attack 普攻动画。角色是远程物理单位，完整身体可见，脚部可见，三分之二俯视伪 3D 视角，手绘 2D 精灵，透明背景，每帧 128x128 PNG。请根据以下 id 生成对应角色：sniper_t2 是二阶精准射手，蓝色射击轨迹；archer_basic 是三阶重型远程干员，琥珀爆裂火花；narantuya 是机动太阳猎手，金色射击轨迹；ray 是束缚光狙击，白金光束；typhon 是重型猎手，冷蓝标记射击；wisadel 是精英饱和火力狙击，青蓝过载弹道。动作包含瞄准、发射、短促后坐力、恢复待机，特效克制不遮挡身体。输出文件用对应角色 id 命名，例如 sniper_t2_attack_000.png 到 sniper_t2_attack_009.png。不要文字、数字、Logo、水印、背景、地面、UI、巨大弹幕、过曝光效、裁切身体、Q版、正面头像、写实 3D。
```

### 术士通用 idle

```text
生成一组 6-8 帧序列帧，用于暗色战术奇幻科幻塔防游戏的术士干员 idle 待机动画。角色是远程法术单位，完整身体可见，脚部可见，三分之二俯视伪 3D 视角，手绘 2D 精灵，透明背景，每帧 128x128 PNG。请根据以下 id 生成对应角色：caster_t2 是二阶过载术士，重外套、发光导管和青紫符文；caster_t3 是三阶链式术士，长法杖、冷白和紫色能量链；ifrit 是直线火焰术士，工业风法杖/喷射器和橙色热光；nymph 是精神崩坏术士，暗色长袍和幽青紫悬浮符印；goldenglow 是自动闪电术士，小型浮游装置和金色电光；logos 是精英处决术士，黑色学者外套、优雅法杖、苍紫虚空符文。动画只做呼吸、法器轻微漂浮、符文微弱闪动，主体居中偏下，底部中心锚点。输出文件用对应角色 id 命名，例如 caster_t2_idle_000.png 到 caster_t2_idle_007.png。不要文字、数字、Logo、水印、背景、地面、UI、过曝魔法、裁切身体、Q版、正面头像、写实 3D。
```

### 术士通用 cast

```text
生成一组 8-12 帧序列帧，用于暗色战术奇幻科幻塔防游戏的术士干员 cast 技能释放动画。角色是远程法术单位，完整身体可见，脚部可见，三分之二俯视伪 3D 视角，手绘 2D 精灵，透明背景，每帧 128x128 PNG。请根据以下 id 生成对应角色：caster_t2 是青紫过载法术；caster_t3 是紫色链式法术；ifrit 是克制橙色线性火焰；nymph 是幽青紫精神冲击；goldenglow 是金色闪电和浮游装置；logos 是苍紫虚空符文。动作包含蓄力、法器/符文增强、释放、恢复，特效必须清楚但不能遮住身体或超出画布。输出文件用对应角色 id 命名，例如 caster_t2_cast_000.png 到 caster_t2_cast_011.png。不要文字、数字、Logo、水印、背景、地面、UI、过曝魔法、裁切身体、Q版、正面头像、写实 3D。
```

### 重装通用 idle

```text
生成一组 6-8 帧序列帧，用于暗色战术奇幻科幻塔防游戏的重装干员 idle 待机动画。角色是防御单位，完整身体可见，脚部可见，三分之二俯视伪 3D 视角，手绘 2D 精灵，透明背景，每帧 128x128 PNG。请根据以下 id 生成对应角色：defender_t2 是二阶强化盾卫，更大盾牌和厚重护甲；defender_t3 是三阶反击盾卫，尖刺盾和精英护甲；saria 是治疗盾卫，医疗设备嵌入盾牌和柔和青蓝治疗灯；penance 是荆棘屏障盾卫，审判者风格和深红屏障主题；jessica_alter 是盾枪战术重装，小型防暴盾和战术枪械；shu 是生长庇护盾卫，植物/田垄意象和优雅盾杖。动画稳定防御姿态，轻微呼吸，盾牌和肩甲小幅移动，主体居中偏下，底部中心锚点。输出文件用对应角色 id 命名，例如 defender_t2_idle_000.png 到 defender_t2_idle_007.png。不要文字、数字、Logo、水印、背景、地面、UI、裁切身体、Q版、正面头像、写实 3D、过强光效。
```

### 重装通用 attack

```text
生成一组 6-10 帧序列帧，用于暗色战术奇幻科幻塔防游戏的重装干员 attack 普攻动画。角色是防御单位，完整身体可见，脚部可见，三分之二俯视伪 3D 视角，手绘 2D 精灵，透明背景，每帧 128x128 PNG。请根据以下 id 生成对应角色：defender_t2 是大盾稳重打击，绿色防御光；defender_t3 是反击盾击，白色反击火花；saria 是医疗盾击，柔和青蓝治疗/冲击光；penance 是深红屏障反击；jessica_alter 是盾枪射击，橙色枪口火光；shu 是绿色生长守护能量。动作包含起手、盾击/短武器/枪击、命中感、恢复防御姿态，角色位置和比例保持一致，特效克制。输出文件用对应角色 id 命名，例如 defender_t2_attack_000.png 到 defender_t2_attack_009.png。不要文字、数字、Logo、水印、背景、地面、UI、巨大冲击波、裁切身体、Q版、正面头像、写实 3D。
```

## 敌人补全

### 源石虫类敌人 idle 和 walk

```text
生成暗色战术奇幻科幻塔防游戏的源石虫/腐化黏液类敌人序列帧，透明背景，每帧 128x128 PNG，三分之二俯视伪 3D 视角，手绘 2D 精灵，主体居中偏下，适配 64px 地图格。请根据以下 id 生成 idle 或 walk 动画：slime 是小型暗紫半透明黏液怪，红紫核心；originium_slug_alpha 是更大的强化源石虫，核心更亮并有小晶体；infused_originium_slug 是不稳定灌注源石虫，有魔法泄漏和爆炸暗示；splitting_originium_slug 是分裂源石虫，膨胀身体内有两个小核心。idle 为 6-8 帧轻微蠕动和核心闪烁，walk 为 6-8 帧压缩伸展向前爬行但主体保持居中。输出文件用对应敌人 id 和动作命名，例如 slime_idle_000.png 或 slime_walk_000.png 开始连续编号。不要文字、数字、Logo、水印、背景、地面、UI、可爱卡通、巨大身体、写实 3D、过强光效。
```

### 猎犬类敌人 idle 和 walk

```text
生成暗色战术奇幻科幻塔防游戏的猎犬类敌人序列帧，透明背景，每帧 128x128 PNG，三分之二俯视伪 3D 视角，手绘 2D 精灵，主体居中偏下，适配 64px 地图格。请根据以下 id 生成 idle 或 walk 动画：hound 是快速荒原猎犬，低伏瘦长暗色身体、红眼、少量晶体碎片；hound_pro 是强化装甲猎犬，有小金属护甲和更锐利攻击姿态。idle 为 6-8 帧低伏呼吸和警觉头部移动，walk 为 6-8 帧低姿态奔跑循环，腿部清楚但不要散出画布。输出文件用对应敌人 id 和动作命名，例如 hound_idle_000.png 或 hound_walk_000.png 开始连续编号。不要文字、数字、Logo、水印、背景、地面、UI、可爱宠物感、巨大身体、写实 3D、过强光效。
```

### 人形敌人 idle walk attack hit death

```text
生成暗色战术奇幻科幻塔防游戏的人形敌人序列帧，透明背景，每帧 128x128 PNG，三分之二俯视伪 3D 视角，手绘 2D 精灵，主体居中偏下，完整身体可见，适配 64px 地图格。请根据以下 id 生成 idle、walk、attack、hit 或 death 动画：soldier 是基础感染士兵，破旧护甲和粗糙武器；crossbowman 是弩手，兜帽和紧凑弩；caster 是敌方法师，长袍和紫色法器；senior_caster 是高级法师，更大长袍和更强紫色魔法；possessed_soldier 是再生附身士兵，暗色血管和微弱恢复光；armored_soldier 是装甲士兵，厚重护甲和钝器；dualstrike_swordsman 是双击剑士，双短刃和敏捷姿态；sarkaz_greatswordsman 是精英大剑士，高大暗甲和大剑。idle 为 6-8 帧，walk 为 6-8 帧，attack 为 6-10 帧，hit 为 3-4 帧，death 为 8-12 帧溃散或倒下且无血腥。输出文件用对应敌人 id 和动作命名，例如 soldier_attack_000.png 开始连续编号。不要文字、数字、Logo、水印、背景、地面、UI、血腥、裁切身体、Q版、正面头像、写实 3D、过强光效。
```

### 重甲敌人 idle walk attack hit death

```text
生成暗色战术奇幻科幻塔防游戏的重甲敌人序列帧，透明背景，每帧 128x128 PNG，三分之二俯视伪 3D 视角，手绘 2D 精灵，主体居中偏下，完整身体可见，适配 64px 地图格。请根据以下 id 生成 idle、walk、attack、hit 或 death 动画：shieldguard 是持盾重甲卫兵，大盾和护盾层；heavy_defender 是超重防御敌人，巨大装甲和盾/装甲板；lumberjack_veteran 是拆建筑伐木老手，粗犷斧头和工具武器；demolitionist 是爆破/锤击拆除者，重装身体和危险背包；siege_breaker 是大型攻城破坏者，巨大锤或撞槌，缓慢不可阻挡。idle 为 6-8 帧，walk 为 6-8 帧沉重步伐，attack 为 6-10 帧强力打击，hit 为 3-4 帧小幅受击，death 为 8-12 帧倒塌或碎裂但无血腥。输出文件用对应敌人 id 和动作命名，例如 shieldguard_walk_000.png 开始连续编号。不要文字、数字、Logo、水印、背景、地面、UI、血腥、裁切身体、Q版、正面头像、写实 3D、过强光效。
```

### 飞行敌人 idle walk attack hit death

```text
生成暗色战术奇幻科幻塔防游戏的飞行敌人序列帧，透明背景，每帧 128x128 PNG，三分之二俯视伪 3D 视角，手绘 2D 精灵，主体居中，不能接触地面，适配 64px 地图格。请根据以下 id 生成 idle、walk、attack、hit 或 death 动画：bat 是小型暗色飞行生物，暗翼和红眼；arts_drone 是飞行法术无人机，机械/法术混合体，青紫核心和短程法术发射器。idle 为 6-8 帧悬停循环，walk 为 6-8 帧飞行动画，attack 为 6-10 帧俯冲或发射，hit 为 3-4 帧摇晃，death 为 8-12 帧坠落或能量熄灭。输出文件用对应敌人 id 和动作命名，例如 bat_idle_000.png 开始连续编号。不要文字、数字、Logo、水印、背景、地面、UI、血腥、可爱卡通、写实 3D、过强光效。
```

### Boss milk_dragon_chief

```text
生成 Boss 敌人 milk_dragon_chief 的序列帧，透明背景，每帧 192x192 PNG，暗色战术奇幻科幻塔防游戏手绘 2D 精灵，三分之二俯视伪 3D 视角，主体居中偏下，底部中心锚点，缩小到 64-96px 后仍有清楚轮廓。Boss 是龙首领/龙化装甲指挥官，巨大角冠或龙角，厚重护甲，暗色鳞片/金属，少量冷青和暗金光。需要生成 idle、walk、attack、hit、death、phase_transition。idle 为 6-8 帧压迫感呼吸，walk 为 6-8 帧沉重移动，attack 为 8-12 帧爪击/武器打击或短程能量冲击，hit 为 3-4 帧受击，death 为 10-16 帧倒下或暗光消散，phase_transition 为 10-16 帧护甲破裂、能量涌动并进入更狂暴二阶段。输出文件用 Boss id 和动作命名，例如 milk_dragon_chief_attack_000.png 开始连续编号。不要文字、数字、Logo、水印、背景、地面、UI、血腥、裁切身体、正面头像、写实 3D、全屏大特效。
```

### Boss patriot

```text
生成 Boss 敌人 patriot 的序列帧，透明背景，每帧 192x192 PNG，暗色战术奇幻科幻塔防游戏手绘 2D 精灵，三分之二俯视伪 3D 视角，主体居中偏下，底部中心锚点，缩小到 64-96px 后仍有清楚轮廓。Boss 是巨大重甲战争领主，持长枪或巨盾，第一阶段厚重防御轮廓，第二阶段更具攻击性，暗钢护甲、冷蓝灰阴影、少量苍白战意光。需要生成 idle、walk、attack、hit、death、phase_transition。idle 为 6-8 帧沉重呼吸，walk 为 6-8 帧缓慢压迫步伐，attack 为 8-12 帧长枪突刺/盾击，hit 为 3-4 帧受击，death 为 10-16 帧跪倒或战意熄灭，phase_transition 为 10-16 帧盾甲破裂、姿态从防御转为进攻。输出文件用 Boss id 和动作命名，例如 patriot_phase_transition_000.png 开始连续编号。不要文字、数字、Logo、水印、背景、地面、UI、血腥、裁切身体、正面头像、写实 3D、全屏大特效。
```

## 建筑补全

### 资源建筑 idle active damaged destroyed

```text
生成暗色战术奇幻科幻塔防游戏的资源建筑序列帧，透明背景，每帧 128x128 PNG，三分之二俯视伪 3D 视角，手绘 2D 建筑精灵，主体居中偏下，底部中心锚点，适配 64px 地图格。请根据以下 id 生成 idle、active、damaged 或 destroyed：lumber_station 是伐木收集站，有木材捆、紧凑锯具和旧金属支架；stone_quarry 是采石站，有小型钻机、岩石碎片和坚固底座；mana_extractor 是魔力萃取器，有水晶舱和青蓝魔法核心。idle 可为 1 帧或 6 帧微循环，active 为 6-8 帧机械/魔法运作，damaged 为破损状态或 4 帧烟火花，destroyed 为 8-12 帧坍塌成碎片。输出文件用对应建筑 id 和动作命名，例如 lumber_station_active_000.png 开始连续编号。不要文字、数字、Logo、水印、背景、地面、UI、过大阴影、写实 3D、过强光效。
```

### 光环建筑 idle active damaged destroyed

```text
生成暗色战术奇幻科幻塔防游戏的光环/辅助建筑序列帧，透明背景，每帧 128x128 PNG，三分之二俯视伪 3D 视角，手绘 2D 建筑精灵，主体居中偏下，底部中心锚点，适配 64px 地图格。请根据以下 id 生成 idle、active、damaged 或 destroyed：medical_station 是战地医疗站，折叠支架、深色金属外壳、柔和青蓝治疗核心，不要红十字；gravity_tower 是重力减速塔，暗色石金属小塔和紫色重力环；inspiring_monolith 是鼓舞方碑，青绿色光槽和信号脉冲；war_shrine 是战争祭坛，紧凑祭坛形体和红橙战斗光。idle 可为 1 帧或 6 帧微循环，active 为 6-8 帧光效脉冲，damaged 为破裂/弯曲状态，destroyed 为 8-12 帧坍塌碎片。输出文件用对应建筑 id 和动作命名，例如 medical_station_idle_000.png 开始连续编号。不要文字、数字、Logo、水印、背景、地面、UI、红十字、过大阴影、写实 3D、过强光效。
```

### wood_wall damaged destroyed

```text
生成建筑 wood_wall 的 damaged 和 destroyed 序列帧，透明背景，每帧 128x128 PNG，暗色战术奇幻科幻塔防游戏手绘 2D 建筑精灵，三分之二俯视伪 3D 视角，主体居中偏下，底部中心锚点，适配 64px 地图格。wood_wall 是临时木制路障，强化木材、金属箍、旧钉板，轮廓宽而稳。damaged 为 1 帧或 4 帧轻微烟尘循环，木板开裂、金属箍弯曲但仍可识别；destroyed 为 8-12 帧坍塌成木板和碎片，最终碎片仍在画布中心。输出文件按 wood_wall_damaged_000.png 或 wood_wall_destroyed_000.png 开始连续命名。不要文字、数字、Logo、水印、背景、地面、UI、爆炸遮挡主体、过大阴影、写实 3D。
```
